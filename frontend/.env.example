# Local dev: same-origin /api (Vite proxies to http://127.0.0.1:8000).
# Do NOT use http://127.0.0.1:8000/api from a page on http://localhost:5173 —
# browsers often block that and login appears to "fail" with no Django log line.
VITE_API_BASE_URL=/api
VITE_GOOGLE_CLIENT_ID=
